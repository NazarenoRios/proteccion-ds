services:
  - type: web
    name: essence-ui-webhook
    env: node
    buildCommand: cd chromatic-webhook && npm install
    startCommand: cd chromatic-webhook && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
